public enum SpawnType
{
    RANDOM_AROUND_PLAYER = 0,
    PLAYER_FORWARD = 1,
    RANDOM_MAP_POSITION = 2,
    MANUAL_PLACEMENT = 4, // Should be manually instantiated in scene
    RANDOM_PREDEFINED_POSITION = 5,
}